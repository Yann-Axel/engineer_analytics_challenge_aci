"""
Air Côte d'Ivoire MCP server — exposes marts + ontology to AI assistants.

Why the package is named `mcp_server` and not `mcp`:
the SDK we depend on is also named `mcp`, and a local package with the same
name would shadow it during imports.
"""
